const accountId=1233445 // constant (Non changable)!
let accountEmail="manku@gmail.com" // let used for variable (changable)!
var accountPassword="12345" // Avoid using var bcoz of block scope and functional scope!
accountCity="jaipur" // variable is written as this type but must use let!

let accountState //Without value it's O/P is undefined!

console.table([accountId,accountEmail,accountPassword,accountCity,accountState]) //used for printing in a tabular form!